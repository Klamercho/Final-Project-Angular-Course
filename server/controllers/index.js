const userController = require('./userController');
const careersController = require('./careersController');
const clientsController = require('./clientsController');

module.exports = {
    userController,
    careersController,
    clientsController
}